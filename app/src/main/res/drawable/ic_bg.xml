<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Bo tròn ở 4 góc ngoài -->
    <corners android:radius="24dp" />

    <!-- Viền nằm bên ngoài -->
    <stroke
        android:width="4dp"
        android:color="#FF9800" />

    <!-- Bên trong -->
    <solid android:color="#FFFFFF" />

</shape>
